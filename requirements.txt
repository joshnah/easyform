# Base requirements for back2 (the current backend).
# All native back2 endpoints + the lightweight legacy compat endpoints
# (/form/text, /pattern/detect, /fill-entries/*, /checkbox-entries/*,
# /pdf/fill, /docx/fill) work with these alone.
#
# For the heavyweight `/context/extract` compat endpoint (OCR + Docling
# layout extraction in back/context_extractor.py), also install
# requirements-extras.txt.

fastapi
uvicorn
PyMuPDF
python-docx
openai
groq
requests
tokenizers

# Used only by the lightweight compat endpoints (back/text_extraction.py):
pypdf
